const Loader = () => {
  return (
    <div className="mt-5 flex justify-center items-center h-full">
      <div className="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-[#fda61e]"></div>
    </div>
  );
};

export default Loader;